# Leave DATABASE_URL empty to use the private PostgreSQL container from compose.yaml.
# Set it only when you intentionally use an external PostgreSQL server.
DATABASE_URL=
SOLVER_TOKEN=replace-with-random-token
RATE_LIMIT_SALT=replace-with-random-salt
WEB_BIND=127.0.0.1
WEB_PORT=3000
